package routes

import (
	"sso-service/controllers"
	middleware "sso-service/middlewares"

	"github.com/gofiber/fiber/v2"
)

func SetupMasterAppRoutes(app *fiber.App) {
	masterapps := app.Group("/master", middleware.JWTMiddleware)
	masterapps.Get("/apps", controllers.GetMasterApps)
}
